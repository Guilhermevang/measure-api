services:
  api:
    build:
      context: .
      target: production
    ports:
      - "${PORT:-8800}:8800"
    env_file:
      - .env
    volumes:
      - meter_storage:/app/storage
    restart: unless-stopped

volumes:
  meter_storage:
